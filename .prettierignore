node_modules/
dist/
cdk.out/
coverage/
.venv/
package-lock.json
uv.lock

# Generated pilot run exports. Immutable output with its own checksum manifest;
# reformatting them would break the checksums they ship with.
.pilot-runs/

# Generated knowledge-graph artifacts. Rebuilt by `/graphify`.
graphify-out/
.pilot-local/
apps/web/test-results/
apps/web/playwright-report/
apps/web/dist/

# The frozen protocol's canonical manifest. Written by `make pilot-freeze` with its
# own digest recorded beside it, so reformatting it would invalidate the freeze.
experiment/pilot/canonical-run-manifest.json

# Per-developer Claude Code settings. Git-ignored, not this repository's to format.
.claude/settings.local.json

# Generated showcase output. `release/showcase-2026/` ships its own `checksums.sha256`
# over every file it contains, so reformatting one would invalidate the digest it was
# published with. The asset sources beside it are written by the capture, chart and
# evidence scripts; reformatting them would show up as a diff on every regeneration.
release/
docs/showcase/assets/source/chart-data/
docs/showcase/assets/source/deployment-evidence.html
docs/showcase/assets/source/screenshot-metadata.json

# Generated by `make pages`; see .gitignore.
site/
